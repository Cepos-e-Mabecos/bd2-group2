component_depth: 1
languages:
- name: python
  production:
    exclude:
      - /tests/.*
      - /api/databaseutils.py
      - /database/.*
      - /frontend/.*
  test:
    exclude:
      - /api/databaseutils.py
      - /database/.*
      - /frontend/.*
    include:
      - /tests/.*